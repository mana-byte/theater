"""Pure rendering modules for the régie lineage tree.

Layout walks the forest, glyphs compose the three-row leaf, and routing
traces sends and awaits across the rail grid. None of these import the
daemon client or tmux.
"""
